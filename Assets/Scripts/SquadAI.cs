using UnityEngine;

public class SquadAI : MonoBehaviour
{

    /// <summary>
    /// Add a bool where, if is player, give player control.
    /// If player is not controlling unit, swap to AI control.
    /// </summary>
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
